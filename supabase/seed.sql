-- Predix — seed for the DEV project only. Never run against prod.
--
-- Skeleton (F0): grows in F2 into a realistic test competition (teams,
-- groups, fixtures with controllable kickoff times) so lock-boundary and
-- conflict tests have a substrate.
--
-- Test users must be created through Supabase Auth (not raw inserts into
-- auth.users); the seed only fills public tables for known user ids.

-- select 'seed placeholder — populated in F2';
